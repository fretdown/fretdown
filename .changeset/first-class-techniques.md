---
'@fretdown/render': patch
---

Render vibrato and palm mute as first-class technique marks: `.vib` now draws VexFlow's wavy
vibrato line (instead of a "vib" text label), and `.pm` draws a "P.M." label lifted clear of
the top string line so it no longer crowds the first fret.
