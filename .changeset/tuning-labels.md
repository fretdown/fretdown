---
'@fretdown/render': minor
---

Show per-string tuning labels in the left gutter of each row (the open-note letter for each
line, top = highest string), and align `computeLayout` measure boxes with the rendered
stave so overlays like the playback cursor sit on the strings.
